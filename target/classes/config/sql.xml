<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="profile">
    <select id="getProfile" resultType="hashmap">
        SELECT *
		FROM public."IMAGE_PROFILE"
    </select>
    
    <select id="getProfileOne" parameterType="Long" resultType="hashMap">
        SELECT *
		FROM public."IMAGE_PROFILE"
		WHERE "IMAGE_PROFILE"."SEQ" = #{seq}
    </select>
    <update id="insertProfile" parameterType="hashmap">
	    INSERT INTO public."IMAGE_PROFILE"(
		 "IMAGE_NAME", "IMAGE_WIDTH", "IMAGE_HEIGHT", "IMAGE_SDATE", "IMAGE_MAXVALUE", "IMAGE_MINVALUE", "IMAGE_AVERAGE")
		VALUES (#{imageName}, #{imageWidth}, #{imageHeight}, #{imageSdate}, #{imageMax}, #{imageMin}, #{imageAvg});
    </update>
<!--     <update id="insertProfile" parameterType="hashmap"> -->
<!-- 	    INSERT INTO public."IMAGE_PROFILE"( -->
<!-- 		 "IMAGE_NAME") -->
<!-- 		VALUES (#{name}); -->
<!--     </update> -->
</mapper>